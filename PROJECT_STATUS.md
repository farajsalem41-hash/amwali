# PROJECT_STATUS — أموالي (Amwali)

آخر تحديث: 2026-09-22

## الحالة العامة

المشروع **مكتمل وظيفيًا** لكل المراحل من 1 إلى 11. لا توجد أخطاء حرجة.
Backend + Frontend يبنون بنجاح، و52 اختبارًا تنجح كلها، وتم اختبار التدفقات الأساسية يدويًا وآليًا في المتصفح.

| البند | الحالة |
| --- | --- |
| `tsc --noEmit` (backend) | نظيف |
| `npm run build` (frontend) | نظيف — index 385kB، Reports 390kB، AdminPanel 16kB (code-splitting) |
| `npm test` (backend) | 52/52 ناجح في 7 ملفات |
| فحص صفحات المتصفح | 0 أخطاء Console/Page على كل الصفحات |
| فحص التدفقات الوظيفية | ناجح (إنشاء طلب، دفعة جزئية، منع الزيادة، رفع إثبات، تأكيد/رفض، توصيل، تقارير، تصدير، فواتير، موظفين، دعم، اشتراك، لوحة الإدارة) |
| Responsive | تم إصلاح تجاوز العرض على 375px وسلوك القائمة الجانبية |

## ما تم إنجازه (المراحل 1–11)

1. **Auth** — تسجيل (4 أنواع حسابات)، دخول، JWT، استعادة كلمة المرور بدون كشف وجود الرقم.
2. **Merchant Profile + Bank Accounts** — بيانات النشاط، الشعار، حسابات بنكية/محافظ.
3. **Payment Requests** — إنشاء/تعديل/إلغاء، خصم، تاريخ انتهاء، رابط عام غير قابل للتخمين، StatusHistory.
4. **Public Payment Page + Proofs** — صفحة دفع عامة بدون تسجيل، رفع إثبات تحويل، لا تكشف أي بيانات داخلية.
5. **Partial Payments** — دفعات متعددة، `recalcPaymentRequest()` مصدر الحقيقة الوحيد، الحسابات في الـBackend فقط، المتبقي ≥ 0 والمدفوع ≤ الإجمالي، الدفعات غير قابلة للتعديل.
6. **Customers + Invoices** — سجل عملاء، فواتير ببنود وخصم وربط بطلب دفع وتحديث تلقائي للحالة.
7. **Products + Orders + Delivery** — منتجات، طلبات بيع، مناديب مع رابط خاص لتحديث حالة التوصيل فقط.
8. **Reports + Exports** — ملخص الفترة، التحصيل اليومي، رسوم بيانية، تصدير Excel وPDF بعربية مُشكَّلة ومُرتَّبة RTL.
9. **Employees + Permissions** — أدوار (مدير/محاسب/كاشير/مخصص)، صلاحيات مفصلة، تقييد الكاشير على طلباته.
10. **Subscriptions + Support + Admin Panel** — خطط، طلب ترقية، تذاكر دعم وردود، لوحة إدارة عليا (تجار، طلبات، دفعات، اشتراكات، تذاكر).
11. **Security Hardening** — عزل كل تاجر عن الآخر، فحوصات IDOR، Rate limiting، Helmet، تدقيق العمليات الحساسة (AuditLog)، حماية الروابط المنتهية/المعطلة، أسرار في `.env` فقط + `.env.example`، نسخ احتياطي للبيانات عبر script.

## إصلاحات هذه الجولة

- `ACCOUNT_TYPE_FEATURES`: إضافة `delivery`/`branches`/`employees` لحساب `online_seller` و`products` لحساب `store` (كان يسبب 403 على المناديب).
- `/auth/me`: إرجاع `isOwner`/`adminRole` وتحديث حالة الواجهة → ظهور صفحة الاشتراك وبوابات المالك.
- Reports/Subscription: إصلاح قراءة نوع الحساب والمالك، وتنسيق القيم النقدية مقابل الأعداد.
- تقسيم الحزمة (lazy) لصفحتي التقارير ولوحة الإدارة.
- تبويبات لوحة الإدارة أصبحت مرتبطة بالمسار (deep-link).
- Mobile: إصلاح تجاوز العرض في لوحة التحكم (`min-w-0` + `.table-wrap`) والتحقق من القائمة الجانبية (مغلقة افتراضيًا، تفتح/تُغلق وتُغلق عند التنقل).
- PDF العربي: إصلاح شكل الحرف قبل ليغاتور «لا»، وإعادة كتابة الترتيب البصري بحيث تبقى الأرقام والتواريخ بترتيبها الصحيح والمسافات في مكانها.

## المتبقي (غير حرج)

- النشر على Vercel (الخطوة التالية بعد تسليم الـZIP).
- إشعارات SMS/بريد فعلية (حاليًا إشعارات داخلية فقط).
- OCR لإثباتات الدفع مُعرَّف كخطوة غير نهائية؛ التأكيد النهائي دائمًا من التاجر.

## آخر خطأ تم إصلاحه

عرض PDF العربي: أرقام التواريخ كانت تُعكس ومسافات مفقودة بين الكلمات — تم إصلاحه في `backend/src/utils/arabic.ts` والتحقق عبر مواضع الكلمات في الملف الناتج.

## كيف تُشغّل المشروع

انظر `README.md` (متطلبات، `.env`، تشغيل MongoDB، seed، تشغيل الـAPI والواجهة، الاختبارات).

## بيانات تجريبية (محلية فقط)

تُنشأ عبر `scripts/seed_demo.py` وتُكتب في `/tmp/amwali-demo.json`. لا توجد أي أسرار حقيقية داخل الكود.
